import React from 'react'
import { MediaSectionContainer } from './building-blocks'

const MediaSection = ({videos}) => {
    return (
        <MediaSectionContainer
            videos={videos}
        />
    )
}

export default MediaSection