import React from 'react'
import { MediaCardInnerContainer } from './media-card'

const MediaCard = ({video}) => {
    return (
        <MediaCardInnerContainer video={video} />
    )
}

export default MediaCard